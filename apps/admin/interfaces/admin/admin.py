"""Django admin registrations for admin models."""

"""Compatibility module.

Admin registrations now live only in ``apps.admin.admin`` to avoid duplicate
ModelAdmin definitions for the same models.
"""
